# Backend API base URL (no trailing /api).
# Copy this file to .env and update only if you want to override the default.
# In localhost, the app now tries /api first and then falls back to this URL.
# Example: https://your-backend-domain.com
VITE_API_URL=https://darkpattern-1.onrender.com
